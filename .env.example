# AWS Configuration
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-west-3

# MongoDB Configuration
MONGO_ROOT_USERNAME=
MONGO_ROOT_PASSWORD=
MONGO_DB_NAME=infynd

# Application Configuration
NODE_ENV=development
ENFORCE_HTTPS=false

# Email Configuration
SES_FROM_EMAIL=noreply@infynd.com

# Cognito Configuration (if needed)
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=

# Kafka Configuration (if needed)
KAFKA_BROKERS=

# Redis Configuration (if needed)
REDIS_HOST=
REDIS_PORT=6379

# JWT Configuration
JWT_SECRET=
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d
